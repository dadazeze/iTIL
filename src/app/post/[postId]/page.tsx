import { getPostById } from '@/shared/services/post';

interface IProps {
  params: {
    postId: string;
  };
}

export default async function PostPage({ params }: IProps) {
  
  // console.log(post)
  return <></>;
}
