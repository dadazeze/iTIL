import { AppBarUI } from '@/shared/components/common/bar/AppBarUI';
import React from 'react';

export default function CommentBarUI() {
    return (
        <AppBarUI>
            
        </AppBarUI>
    );
};
